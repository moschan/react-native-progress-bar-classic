{
  "name": "ProggressBarExample",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "react-native start",
    "sync": "rm -rf ./node_modules/react-native-progress-bar-classic; sane '/usr/bin/rsync -v -a --exclude .git --exclude example --exclude node_modules ../ ./node_modules/react-native-progress-bar-classic/' .. --glob='{**/*.json,**/*.js}'"

  },
  "dependencies": {
    "react": "15.0.2",
    "react-native": "^0.26.3",
    "react-native-progress-bar-classic": "^1.2.0"
  }
}
